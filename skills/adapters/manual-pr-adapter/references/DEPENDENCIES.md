# Manual PR Adapter Dependencies

## Required skills

- `worker-contract` — canonical worker return format.

## Required tools

- `read`, `write`, `bash` for file handling.

## Required binaries

- Python 3.x for CSV/JSON parsing.

## Environment

- No environment variables are required.
- Optional file paths may be provided in config.
