# Dependencies

## Required

- **Python 3.10+** — scripts are written in Python 3.
- **git** — required for creating and managing worktrees.

## Required skills

- `worker-contract` — return contract used if the block is invoked through a subagent.
- `context-reports` — conventions for context directory layout and report schemas.
- `detect-project-context` — locate the project root and git repository.

## Recommended

None.

## Optional

None.

## Binaries

- `git`
- Python 3.10+

## Consumed references

- The git repository at `repo`.

## Produced artifacts

- Temporary git worktree directories (removed by default).
