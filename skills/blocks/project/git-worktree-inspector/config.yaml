# Config declaration for git-worktree-inspector

# This block does not require project-level config. It accepts repo, branch,
# base, and commands as input on each invocation.

skill: []

requires_setup: false
