# Dependencies

## Required

- **Python 3.10+** — scripts are written in Python 3.
- **PyYAML** — required for parsing report frontmatter.
- **git** — used to auto-detect current branch and commit when not provided.

## Required skills

- `worker-contract` — return format if invoked through a subagent.
- `context-reports` — conventions for report frontmatter and freshness vocabulary.

## Recommended

None.

## Optional

None.

## Consumed artifacts

- Context report files (markdown with frontmatter).
- Evidence-store entries (JSON/dict).

## Produced artifacts

None. This block is read-only.
