# Dependencies

## Required skills

- `pr-adapter-contract` — adapter interface contract.
- `worker-contract` — return contract format.

## Optional tools

- `read` and `write` tools for file-based modes.
- Python 3.x for CSV/JSON parsing if needed.

## No runtime tokens

This adapter does not call external APIs. It may read files provided by the user or conductor.
