# Dependencies

## Required skills

- **parse-skill-frontmatter** — extracts canonical frontmatter fields from discovered `SKILL.md` files.

## Required tools and capabilities

- **Read filesystem** — the script scans canonical skill directories and reads `SKILL.md` files.
- **Python 3.x** — the script is written in Python and uses only the standard library.

## Required binaries

- `python3` (or equivalent Python 3 interpreter)

## Required MCP servers

None.

## Environment variables

None.

## Optional dependencies

None. The script uses only `pathlib`, `json`, `argparse`, and `importlib` from the Python standard library.
