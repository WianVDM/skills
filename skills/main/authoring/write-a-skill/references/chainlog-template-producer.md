# {skill-name} chainlog declaration

This document declares how `{skill-name}` produces observations for the [`chainlog`]({chainlog_pattern_path}) pattern.

## Classification

`{skill-name}` is a **producer** of chainlog observations.

Rationale: {why this skill appends observations}.

## Work item types

This skill produces observations for the following work item types:

- {work_item_type, e.g., `ticket`, `pr`, `branch`, `commit`}

## Produced capabilities

| Capability | Source tool/adapter | When appended | Capability contract |
| ---------- | ------------------- | ------------- | --------------------- |
| {capability-name} | {source, e.g., `pr-adapter`} | {workflow step} | {contract path or link} |

## Workflow integration

After collecting data from a tool or subagent, `{skill-name}` normalizes the output to the relevant capability contract and appends it to the chainlog via `chainlog/append`.

Example `append` invocation:

```json
{
  "operation": "append",
  "context_dir": ".agents/context",
  "entry": {
    "work_item_type": "{work_item_type}",
    "work_item_key": "{work_item_key}",
    "capability": "{capability-name}",
    "source": "{source}",
    "source_version": "{version}",
    "schema_version": "{schema-version}",
    "collected_at": "{timestamp}",
    "confidence": "high",
    "payload": { ... }
  }
}
```

Workflow steps:

1. {workflow step one}
2. {workflow step two}
3. Normalize output and append to chainlog via the invocation above.
4. {continue workflow}

## Report linkage

Reports or views generated by `{skill-name}` link back to the chainlog observations they were built from. Each observation includes a `related_report` field pointing to the generated view.

## Storage adapter

`{skill-name}` discovers available storage adapters via `tool-discovery` for the `chainlog-storage` capability. The file-based adapter is the default and the fallback. The selected adapter is recorded below.

| Adapter | Status | Fallback |
| ------- | ------ | -------- |
| file-based (default) | default | none |
| {richer adapter name} | {detected / not detected} | file-based |

## Freshness rules for consumers

Consumers of these observations should treat them as stale when:

- {source change condition, e.g., a new commit is pushed}
- The schema version is incompatible.
- A staleness marker is appended.
- The observation predates the configured freshness window.

## Secrets

No secret values are stored in chainlog observations. Only env-var names or references are used.

## Decisions

| Decision | Rationale |
| -------- | --------- |
| Classification: producer. | {why} |
| Capabilities produced: {list}. | {why} |
| Storage adapter: {choice}. | {why} |

## Related

- [`chainlog` pattern]({chainlog_pattern_path})
- [`chainlog-contract.md` reference]({chainlog_contract_path})
- [`tool-discovery` building block]({tool_discovery_path})
