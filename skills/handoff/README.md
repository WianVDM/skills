# handoff

Write a session handoff snapshot so a fresh session can continue.

Usage: `handoff` or `handoff {key}`

The snapshot is written under the detected project context directory. Use `scripts/handoff-helper.py` to resolve the exact path.
