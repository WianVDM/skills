---
handoff_version: "2.0"
sequence: 1
previous_handoff: null
timestamp: "2026-07-03T11:30:28Z"
session_id: "<current-session>"
generated_by: "handoff skill v2.0"
summary: "Refactored handoff skill from summary tool into a lean, standalone session-continuity skill. Tested execution via /skill:handoff."
---

## Goal

Refactor the `handoff` skill from a conversation-summary tool into a barebones, standalone, session-continuity skill that preserves context across chained sessions so a fresh agent can continue exactly where the previous session stopped.

## Task list

- [x] Clarify intent with user (continuation, not summary; chaining; no hardcoded skill refs).
- [x] Produce design doc and self-audit using `write-a-skill`.
- [x] Draft `SKILL.md`, `README.md`, and `scripts/handoff-helper.py`.
- [x] Identify and fix invocation issue (`/skill:handoff` loaded but did not execute).
- [x] Lean out `SKILL.md` and move reference detail to `README.md`.
- [x] Replace project-specific ticket example with generic `ISSUE-123`.
- [x] Execute test handoff via `/skill:handoff`.
- [ ] Validate the produced handoff in a fresh session.
- [ ] Implement trigger/behavior evals after real-world use.

## What has been tried

- Ran a full `write-a-skill` Review on the original handoff skill; identified 15 Red blockers.
- Drafted v2.0 with explicit frontmatter, chainable schema, deterministic helper script, and context discovery.
- First attempted to fix invocation by setting `disable-model-invocation: false` and adding a dedicated `Invocation` section. User rejected this as bloat and asked me to use `write-a-skill` instead of guessing.
- Reverted the unverified change and ran a focused invocation review. Root cause: `SKILL.md` read like a manual ("When to use" before workflow), so the agent didn't execute on load. The frontmatter was already correct.
- Restructured `SKILL.md` to put the executable workflow first and moved schema/failure modes/security to `README.md`.

## What failed / blockers

- Initial invocation fix was incorrect (changed frontmatter instead of structure).
- User had to intervene to correct the approach.
- No remaining blockers.

## What is next

- Complete this test handoff and report the path.
- Verify in a fresh session that the handoff document is sufficient to continue the refactor work.
- Schedule trigger and behavior evals after real-world usage.

## User clarifications

- Handoff is a continuation artifact, not a conversation summary.
- Must preserve context across 5+ chained sessions.
- Must be barebones and standalone; no hardcoded references to skills like `verify-branch`.
- Must ingest any context generated in the session (subagents, skills, user references).
- `/handoff` and `/skill:handoff` must work with no arguments.
- `Out of scope` should remain in `SKILL.md`.
- `SKILL.md` should be lean; details go to `README.md`.
- Ticket examples should be generic (`ISSUE-123`).

## User preferences

- Practicality over strict standards adherence when they conflict.
- Minimal, lean skill design; avoid bloat.
- Use `write-a-skill` to verify before making changes.
- Prefer small, targeted edits over large rewrites.

## Referenced artifacts

- `skill-design/handoff-intent.md` — captured user intent and constraints.
- `skill-design/handoff-design.md` — v2.0 design doc.
- `skill-review/handoff-audit.md` — initial audit with 15 Red blockers.
- `skill-review/handoff-self-audit.md` — design self-audit.
- `skill-review/handoff-final-audit.md` — final audit of the lean draft.
- `skill-review/handoff-invocation-review.md` — focused invocation review.
- `skills/handoff/SKILL.md` — executable skill contract.
- `skills/handoff/README.md` — reference detail and schema.
- `skills/handoff/scripts/handoff-helper.py` — deterministic helper script.

## Recommended skills

- `write-a-skill` — for future skill reviews, designs, and audits.

## Chain history

This is the first handoff in the unticketed chain.
