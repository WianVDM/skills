{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agentskills.io/schemas/skill-frontmatter.json",
  "title": "Skill Frontmatter",
  "description": "YAML frontmatter schema for SKILL.md. The portable core of a skill is defined by this frontmatter and the markdown body. All portable fields must influence routing, invocation, or runtime behavior.",
  "type": "object",
  "required": ["name", "description", "invocation"],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "minLength": 1,
      "maxLength": 128,
      "description": "Stable identifier. Must match the skill directory name and use lowercase letters, digits, and hyphens only."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "maxLength": 1024,
      "description": "One-sentence routing surface. Tells the agent when to load the skill."
    },
    "invocation": {
      "type": "string",
      "enum": ["model-invoked", "user-invoked"],
      "description": "How the skill is reached. model-invoked skills are held in context for autonomous routing; user-invoked skills are only reachable by name. Required because it determines whether the skill pays context load or cognitive load."
    },
    "allowed-tools": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Harness hint: tools the skill is allowed to use."
    },
    "disallowed-tools": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Harness hint: tools the skill must not use."
    },
    "disable-model-invocation": {
      "type": "boolean",
      "description": "Harness hint: true is equivalent to invocation: user-invoked."
    },
    "globs": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Harness hint: file globs that scope the skill."
    },
    "paths": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Harness hint: explicit paths that scope the skill."
    },
    "depends": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Harness hint: skill names that this skill depends on. The Vercel skills CLI installs these dependencies automatically. Not a portable dependency declaration; use skills.json for that."
    }
  },
  "additionalProperties": true,
  "allOf": [
    {
      "description": "If disable-model-invocation is true, invocation must not disagree.",
      "if": {
        "properties": { "disable-model-invocation": { "const": true } },
        "required": ["disable-model-invocation"]
      },
      "then": {
        "not": {
          "properties": { "invocation": { "const": "model-invoked" } },
          "required": ["invocation"]
        }
      }
    }
  ]
}
